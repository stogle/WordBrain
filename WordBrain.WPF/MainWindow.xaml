<Window x:Class="WordBrain.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WordBrain.WPF"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        FocusManager.FocusedElement="{Binding ElementName=Grid}"
        Height="800"
        Title="WordBrain"
        Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="400"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition MinHeight="400"/>
        </Grid.RowDefinitions>

        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Content="_Grid:"
                   Margin="0,0,5,0"
                   Target="{Binding ElementName=Grid}"
                   VerticalAlignment="Center"/>
            <TextBox x:Name="Grid"
                     Grid.Column="1"
                     FontFamily="Consolas"
                     FontSize="14"
                     HorizontalAlignment="Stretch"
                     Margin="0,5"
                     Text="{Binding Grid, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center"/>

            <Label Grid.Row="1"
                   Content="_Solution:"
                   Margin="0,0,5,0"
                   Target="{Binding ElementName=Solution}"
                   VerticalAlignment="Center"/>
            <ItemsControl x:Name="Solution"
                          Grid.Row="1"
                          Grid.Column="1"
                          Focusable="False"
                          ItemsSource="{Binding Solution}"
                          Margin="0,2.5">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="local:WordViewModel">
                        <xctk:IntegerUpDown Margin="0,2.5,5,2.5"
                                            Maximum="{Binding DataContext.Letters.Count, RelativeSource={RelativeSource AncestorType=local:MainWindow}}"
                                            Minimum="1"
                                            Value="{Binding Length}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <local:PuzzleControl Grid.Row="1"
                             Margin="5"/>

        <Button Grid.Column="1"
                Command="{Binding Solve}"
                Content="Solve"
                IsDefault="True"
                Margin="5"/>

        <ListBox Grid.Column="1"
                 Grid.Row="1"
                 FontFamily="Consolas"
                 FontSize="14"
                 ItemsSource="{Binding Output}"
                 Margin="5"/>
    </Grid>
</Window>
